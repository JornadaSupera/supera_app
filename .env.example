# Copie para .env e preencha quando o projeto Supabase estiver criado.
# Enquanto isso, o app usa src/services/mockApi.js (dados mockados) e
# src/services/supabaseClient.js exporta `null` — nada quebra sem isso.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
